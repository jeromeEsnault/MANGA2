//const bcrypt = require('bcrypt');
//const User = require('../../database/models/user')


// comparer les donne pour la validation
/*bcrypt.compare(User, function(err, result) {
    if (err) {
        console.log(`une erreur s'est produite fait attention a la syntaxe`);

        res.redirect("/")

    } else {
        
    }
});**/
/*bcrypt.compare(User, function(err, result) {
    // result == false
});
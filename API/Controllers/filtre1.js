const { patch } = require('request')
const Manga = require('../database/models/Manga')
const Tome = require('../database/models/Tome')



module.exports = {
  filtre: (res, req) => {
   /* var query = "manga2";
    console.log(query);
    const mangaf = Manga.find({ 'titlevf': query })
    console.log(mangaf)
    if (err) throw err
    else console.log(mangaf)*/

  }
}